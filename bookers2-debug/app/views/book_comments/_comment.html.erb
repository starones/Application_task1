  <table>
            <% book.book_comments.each do |book_comment| %>
        <tr>
            <td><%= link_to user_path(book.user.id) do %>
            <%= attachment_image_tag(book.user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %><br>
            <%= book.user.name %></td>
            <% end %>

            <td><%= book_comment.comment %></td>
            <% if book_comment.user == current_user %>
            <td><%= link_to "Destroy", book_book_comment_path(book_comment.book, book_comment), method: :delete, remote: true, class: "btn btn-sm btn-danger destroy_book_#{book.id}" %> </td>
            <% end %>
            <% end %>
        </tr>
      </table>

        <div class"form-group">
        <%= form_with(model:[book, @book_comment]) do |f| %>
        <%= f.text_area :comment, rows:'4', class: "form-control introduction" %> <br />
        <%= f.submit "送信" %>
        <% end %>
       </div>